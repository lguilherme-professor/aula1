//para criar um repositorio
//entrar na pasta escolhida
//botao direito
//git bash here

//dentro da pasta

git init -> enter

git status -> //para checar se o repositorio está atualizado

git add "nome do arquivo" -> enter //para incluir os arquivos no repositorio

git add . -> adiciona varios arquivos

//para enviar para o git hub
git commit -m "commit inicial"

git config --global user.email "seu e-mail"
git config --global user.name "seu nome"

git commit -m "commit inicial"

git push -> //vai dar erro, tem q configurar para onde mandar os arquivos no git hub

//criar o repositorio no github

git push -> vai dar erro, precisa definir para qual branch vc deve enviar

git push --set-upstream origin master

//muda algo no codigo

git status

git add .

git commit -m "adicionei uma linha"

git push


=========================================

git reflog -> // verifica as versoes do arquivo

$ git reset --hard numero da versao //volta para a versao que vc deseja


//definir o conceito de branch

git branch // mostra as branches

git branch staging //cria a branch staging(dev)

git checkout staging // muda para a branch staging

//conceito de merge

git chekout master //muda para a branch master

git pull -> //puxa o codigo atual da branch master para evitar de sobrescrever codigo antigo

git merge staging // manda as alteracoes da staging para a master

git push

git checkout -b sistema-de-login master

git branch

git add .

git status

git commit -m "criado sistema-de-login"

git checkout master

git pull

git merge sistema-de-login

git push

===========================
//explicar o conceito de pull request





 